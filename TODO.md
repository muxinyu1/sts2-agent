

```
继续修改agent，现在我需要优化战斗对局：
 对于同一个floor的战斗，让大模型玩多次（暂时玩5次，这个参数可以从配置文件调节），记录每次玩的战损（hp减少量），把战损字段也加入到轨迹采集中，由于同一个floor战斗开始时的state应该是相同的，所以添加给轨迹添加一个字段floor_id（对state的hash），用于区分不同state。（玩多次的方法在API.md应该有一个撤销动作的方法，如果没有的话请告诉我，我来看）
```

